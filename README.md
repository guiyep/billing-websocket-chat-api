we use babel to transpile the js so we can use all the latest javascript features

we can use `import aaa from 'anything'` and will use commonjs as default

`npm install` first.

`npm run start` will run everything from index.js